#pragma once

// Compatibility include. New code should include runtime/backend.h and, when
// it needs the CPU implementation, backends/cpu/backend.h explicitly.
#include "runtime/backend.h"
#include "backends/cpu/backend.h"
